# This file contains configuration defaults for base/local environments
# By default, all containers are deployed on the same host. See `docker-compose.prod-dbonly.yml`
# and the README to deploy individual services
---

services:
  nginx-php-moodle:
    ports:
      - "8080:80"
      - "8443:443"
