---

version: '2'

services:
  nginx:
    build: .
    ports:
      - "80:80"
      - "443:443"
    links:
      - fpm
    volumes:
      - /opt/moodle/ssl:/etc/nginx/ssl
  fpm:
    image: php:fpm
    volumes:
      - /opt/moodle:/opt/moodle
      - /opt/moodledata:/opt/moodledata
